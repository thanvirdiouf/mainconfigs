#!/usr/bin/env bash
# simple placeholder: Waybar will show icon and clicking runs wlogout (configured in config).
# This script just returns an icon and tooltip.
jq -n '{text:"", tooltip:"Power menu (click to open wlogout)"}'

